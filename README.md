
  # Create High-Fidelity Prototype

  This is a code bundle for Create High-Fidelity Prototype. The original project is available at https://www.figma.com/design/05WWsPnXMmQKYJN6dcFqXz/Create-High-Fidelity-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  